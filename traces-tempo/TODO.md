# TODO: подготовка к публикации

Гайд **уже живой**: https://traces-tempo-guide.onrender.com/. Сайт появился заодно с envoy -
блюпринт Render разворачивает все сервисы из `render.yaml` сразу, а не по одному. Но к посту гайд
не готов: `<head>` не соответствует обязательному минимуму (`../DESIGN.md` §5), отклонение
записано в реестр §6 там же.

**Ссылку нельзя публиковать и нельзя светить в чатах и в Post Inspector, пока не сделаны
пункты 1 и 2.** Превью LinkedIn кешируется по URL надолго: скрейп страницы без OG-тегов застрянет
в ленте пустой карточкой, и переделать это для того же URL уже не выйдет.

## 1. OG-теги

- [ ] `og:type`, `og:url`, `og:title`, `og:description`, `og:image` (+ `width`/`height`/`alt`),
      `og:site_name`, `og:locale`, `twitter:card` - **абсолютными** URL на
      `traces-tempo-guide.onrender.com`. Относительные LinkedIn молча игнорирует.
- [ ] `<meta name="description">` - фолбэк, если `og:description` не подхватится.
- [ ] Заголовок и описание брать из обещания гайда (первый абзац `README.md`), а не писать
      заново - и без клише из `../DESIGN.md` §4.

## 2. Превью

- [ ] `og-preview.png` **1200×627**, лежит в папке гайда (сборки нет, генерировать нечем).
      Снимать на ширине 1400px и кропить.
- [ ] Кадр должен показывать, что гайд интерактивный: линза с раскрытым пультом и живыми
      числами убедительнее общей схемы. Пересматривать, если этот разрез потом переделают -
      скриншот тихо устаревает.

## 3. Мелочи в `<head>`

- [ ] `favicon.ico` + `favicon.png`. Сейчас консоль гайда даёт 404 на favicon - у envoy он
      закрыт, так что это последнее место в серии, где ошибка в консоли осталась.

## 4. После деплоя тегов

- [ ] Прогнать https://traces-tempo-guide.onrender.com/ через
      [Post Inspector](https://www.linkedin.com/post-inspector/) - и только потом пост.
- [ ] Убрать строку про этот долг из реестра отклонений `../DESIGN.md` §6 и из «Отклонения и
      открытые вопросы» в `README.md`.

---

**Не забыть при любой правке ассетов:** бампнуть `?v=N` во всех местах `index.html` - точные
места даёт `grep -n '?v=' index.html` (сейчас `<link>` + три тега script).
