# TODO: подготовка к публикации

Что нужно сделать перед постом со ссылкой на гайд (LinkedIn). Задачи независимы, кроме
блока с OG-тегами - там нужен финальный URL. `README.md` - про структуру, `DESIGN.md` - про стиль,
этот файл - только про публикацию.

## 1. Хостинг

Артефакт статический (176 КБ, без сборки), но **http-сервер обязателен**: слои подгружаются
через `fetch('layers/*.html')`, на `file://` доска останется пустой. Диплинки живут на
хеше (`app.js:271-287`, вида `#policies/securitypolicy`) и разбираются на клиенте -
серверные rewrite-правила не нужны, ссылка на конкретный узел заработает на любом CDN.

- [x] Хостинг - **Render**, статический сайт из общей репы. Блюпринт `../render.yaml`,
      сервис `envoy-gateway-guide` (`rootDir` + `buildFilter`, сборки нет).
- [x] URL зафиксирован: `https://envoy-gateway-guide.onrender.com/`. Свой домен не брали -
      если возьмём, ссылка из уже опубликованного поста будет вести на старый URL, а превью
      LinkedIn закешируется именно на него. Менять - только до первого поста.
- [x] Задеплоен: https://envoy-gateway-guide.onrender.com/ - сервис `srv-d9q9o3jm8hqs73e9l1v0`
      в воркспейсе `Workspace1`, создан блюпринтом из дашборда (через MCP нельзя - там нет
      `rootDir` и `buildFilter`). Живая страница и OG-теги проверены.

## 2. OG-теги

- [x] `og:type`, `og:url`, `og:title`, `og:description`, `og:image` (+ `width`/`height`/`alt`),
      `og:site_name`, `og:locale`, `twitter:card` - абсолютными URL на `envoy-gateway-guide.onrender.com`.
- [x] Превью `og-preview.png` **1200×627** - слой «Ресурсы» с раскрытой панелью узла Gateway
      (видно, что гайд интерактивный, а не картинка).
- [ ] Прогнать урл через [Post Inspector](https://www.linkedin.com/post-inspector/)
      **после деплоя и до публикации**: LinkedIn кеширует превью по URL надолго, неудачный
      первый скрейп застрянет в ленте.

## 3. Мелочи в `<head>`

- [x] `favicon.ico` + `favicon.png` - брендовый штрих на холсте темы. Консоль теперь чистая
      полностью, без единственного ранее допустимого 404.
- [x] `<meta name="description">` - есть, фолбэк если `og:description` не подхватится.

## 4. Решить осознанно

- [ ] Язык надзаголовка. При `lang="ru"` первое, что видно в превью -
      `Reference · Kubernetes networking`, дальше всё по-русски. Не ошибка, но стоит
      определиться, на кого пост: смешанная аудитория LinkedIn или русскоязычная.

---

**Не забыть при любой правке ассетов:** бампнуть кэш-бастинг во всех местах `index.html` -
`?v=N` у `styles.css`, у `data.js` и константа `V` в bootstrap (сейчас 27). Точные места -
`grep -n '?v=' index.html`.
