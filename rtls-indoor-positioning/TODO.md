# TODO: RTLS

Незакрытый долг этого гайда. Закрыли пункт - удалите его отсюда в том же коммите.

## Публикация

- [x] Сервис `rtls-guide` добавлен в `render.yaml` и строка в таблицу корневого `README.md`.
- [x] OG-теги с абсолютными URL на собственный хост (`rtls-guide.onrender.com`).
- [x] `og-preview.png` 1200×627 - скриншот слоя «Что это» со схемой анатомии.
- [ ] **Blueprint-инстанс создан через дашборд Render.** MCP-инструмент `create_static_site`
      не принимает ни `rootDir`, ни `buildFilter`, поэтому сервис, созданный им, не
      блюпринт-управляемый, и `render.yaml` становится фикцией. Только Dashboard →
      New Blueprint Instance (или Sync существующего блюпринта).
- [ ] **После пуша - `trigger_deploy`.** Пуш сам сборку не запускает: вебхук GitHub до Render
      не доходит, все деплои в истории имеют `trigger: "api"`.
- [ ] **Проверить, что живо именно то, что запушено:** сверить `commit.id` деплоя и
      `curl` живого URL на строку, которая есть только в новой сборке. `list_deploys` со
      статусом `live` о версии не говорит ничего.
- [ ] **Последним шагом** - прогнать URL через
      [LinkedIn Post Inspector](https://www.linkedin.com/post-inspector/), и только потом
      пост. До этого URL нельзя светить там, где ссылки разворачиваются.

## Содержание

- [ ] **Проверить Bluetooth Channel Sounding при следующем обновлении.** Сейчас описан
      будущим временем (Bluetooth 6.0, сентябрь 2024): в стандарте есть, готового
      RTLS-железа на объектах - нет. Когда появится, переписать в настоящем времени и
      обновить строку в матрице.
- [ ] **Профили `RADIO` стоит перепроверить по открытым источникам** (не даташитам
      конкретных вендоров) - особенно токи потребления BLE-меток и шаг сетки для AoA.
      Сейчас это типичные значения класса, помеченные как порядок величины.
- [ ] Кейсы охватывают склад, производство, здравоохранение и опасный объект. Ритейл,
      спорт и стройка сознательно не взяты: шесть карточек - предел наглядности, а лестница
      точности уже покрыта. Если добавлять - только заменой, не расширением.

## Техдолг

- [ ] `favicon.ico` / `favicon.png` скопированы у донора. Свой знак не нарисован - консоль
      чистая, но иконка чужая.
- [ ] Геометрия на слое «Методы» нарисована координатами вручную. Точка пересечения лучей
      посчитана на бумаге; если менять положение анкеров, пересчитывать и её (метка обязана
      стоять именно в пересечении, иначе схема врёт).
